{
  "hooks": [
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/validate-cloud-command.py"
        }
      ]
    },
    {
      "event": "SessionStart",
      "matcher": "*",
      "hooks": [
        {
          "type": "command",
          "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"contextForAgent\":\"DeployShield is active. Write/mutating operations for the following cloud CLIs are blocked: aws, gcloud, az, kubectl, helm, terraform, pulumi. Only read-only commands are allowed. Suggest --dry-run flags and terraform plan instead of terraform apply where appropriate.\"}}'"
        }
      ]
    }
  ]
}
